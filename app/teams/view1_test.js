'use strict';

describe('myApp.teams module', function() {

  beforeEach(module('myApp.teams'));

  describe('teams controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});